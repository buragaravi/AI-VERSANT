# Production deployment configuration
# This file tells the deployment platform how to run your application

# For platforms like Heroku, Railway, Render, etc.
web: gunicorn --config gunicorn_unix_config.py application:application

# Alternative configurations for different platforms:
# web: gunicorn --workers 4 --worker-class gevent --worker-connections 2000 --bind 0.0.0.0:$PORT application:application
# web: gunicorn --config gunicorn_config.py wsgi:application
# web: python start_unix_production.py
