# Study Edge - Backend Requirements
# Build system dependencies (required for Python 3.13)
setuptools>=68.0.0
wheel>=0.41.0
build>=1.0.0
# Core web framework and extensions
Flask>=2.3.0
Flask-JWT-Extended>=4.6.0
Flask-CORS>=4.0.1
# Database
Flask-PyMongo>=2.3.0
pymongo[srv]>=4.8.0
# Security and utilities
bcrypt>=4.0.1
python-dotenv>=1.0.1
# AWS services
boto3>=1.34.0
botocore>=1.34.0
# AI services
openai>=1.3.0
# File processing
openpyxl>=3.1.2
# Testing
pytest>=7.4.0
# Deployment and server
gunicorn>=22.0.0
Werkzeug>=2.3.0
# HTTP and utilities
python-multipart>=0.0.6
requests>=2.31.0
python-dateutil>=2.8.0
pytz>=2022.7
# Text-to-speech and audio generation (required for LISTENING module)
gTTS>=2.5.0
pydub>=0.25.0
# Audio processing dependencies (required for LISTENING module)
numpy>=1.26.0
scipy>=1.11.0
librosa>=0.10.0
# Authentication and security
flask-bcrypt>=1.0.1
# Email service
brevo-python>=1.1.0
# Template engine
jinja2>=3.1.0
# Speech recognition and audio processing
SpeechRecognition>=3.10.0
ffmpeg-python>=0.2.0
# API and real-time communication
fastapi>=0.104.0
uvicorn>=0.24.0
python-socketio>=5.10.0
flask-socketio>=5.3.0
eventlet>=0.33.0
# Task scheduling
apscheduler>=3.10.0

# Note: For audio generation to work properly, you may also need to install system dependencies:
# - ffmpeg (for audio file processing)
# - portaudio (for audio recording)
# - libasound2-dev (for ALSA audio support on Linux)
# 
# On Ubuntu/Debian: sudo apt-get install ffmpeg portaudio19-dev libasound2-dev
# On CentOS/RHEL: sudo yum install ffmpeg portaudio-devel alsa-lib-devel
# On macOS: brew install ffmpeg portaudio
# On Windows: Download ffmpeg from https://ffmpeg.org/download.html
#
# Python Version Compatibility Notes:
# - This requirements file is optimized for Python 3.11+ and Python 3.13
# - Some packages may require building from source on Python 3.13
# - If you encounter build issues, consider using Python 3.11 or 3.12
# - Alternative packages for Python 3.13 compatibility:
#   - For numpy: use numpy>=1.26.0 (has Python 3.13 wheels)
#   - For scipy: use scipy>=1.11.0 (has Python 3.13 wheels)
#   - For librosa: use librosa>=0.10.0 (has Python 3.13 wheels)
#
# Alternative Requirements Files:
# - requirements_python313.txt: Python 3.13 specific with compatible versions
# - requirements_minimal.txt: Minimal requirements without audio processing (for troubleshooting)
# - If you encounter build issues, try: pip install -r requirements_python313.txt
# - For minimal setup: pip install -r requirements_minimal.txt 